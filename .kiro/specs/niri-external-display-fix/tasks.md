# 実装計画

- [x] 1. 角丸ウィンドウルールの無効化
- [x] 1.1 `geometry-corner-radius` と `clip-to-geometry` を含むウィンドウルールを `/-` でコメントアウトする
  - 現在有効になっている角丸ウィンドウルールブロックの先頭に `/-` プレフィックスを追加して無効化する
  - マルチディスプレイ環境（異なるスケーリングファクター）でのホットプラグ時にクラッシュを引き起こすため無効化する旨のコメントを追加する
  - `prefer-no-csd` が無効のまま（コメントアウト状態）であることを確認する
  - _Requirements: 1.1, 1.3, 2.1, 2.2, 2.3_

- [x] 2. デバッグセクションのコメント文書化と追加オプション検討
- [x] 2.1 (P) `disable-monitor-names` の目的を説明するコメントを追加する
  - niri 0.1.9/0.1.10で同一make/model/serialのモニター接続時にクラッシュするバグの回避策であることをコメントに記載する
  - 将来のniriバージョンで修正された場合に削除可能であることを明記する
  - _Requirements: 3.1_

- [x] 2.2 (P) `wait-for-frame-completion-before-queueing` デバッグオプションを追加する
  - 外部モニター接続時のフリッカー防止のためにこのオプションをdebugセクションに追加する
  - 外部ディスプレイが「映らない」症状がフリッカーに起因する場合に有効であることをコメントで説明する
  - _Requirements: 1.1, 1.2_

- [x] 3. configuration.nixのカーネルパラメータコメント整理
- [x] 3.1 (P) i915カーネルパラメータのコメントを充実させる
  - 各パラメータ（`i915.enable_psr=0`, `i915.enable_fbc=0`, `i915.enable_dc=0`）の目的と、niriの外部ディスプレイホットプラグ安定性との関連を説明するコメントを追加する
  - `boot.initrd.kernelModules` のearly KMS設定についても、なぜ必要かの説明コメントを確認・補足する
  - _Requirements: 3.2_

- [x] 4. output設定の確認と検証手順の追加
- [x] 4.1 (P) eDP-1のoutput設定がコメントアウト済みであること、外部ディスプレイ用の明示的なoutput設定がないことを確認する
  - niriの自動検出機能にディスプレイ設定を委ねる現在の方針が適切であることを確認する
  - `niri msg outputs` コマンドで出力情報が確認できることをコメントに記載する
  - _Requirements: 4.1, 4.2, 4.3_

- [x] 5. 回避策の検証手順ドキュメントの作成
- [x] 5.1 config/niri/WORKAROUNDS.md を作成し、現在適用されている全回避策を一覧化する
  - niri config.kdlのデバッグオプション（`disable-monitor-names`, `wait-for-frame-completion-before-queueing`）を記載する
  - 角丸ウィンドウルールの無効化とその理由を記載する
  - configuration.nixのi915カーネルパラメータを記載する
  - 各回避策について、削除条件（どのniriバージョンで修正されたか、どのように検証するか）を明記する
  - _Requirements: 3.3_

- [x] 5.2 niriバージョンアップ時の段階的検証手順を文書化する
  - 回避策を一つずつ無効化してテストする手順を記載する（角丸の再有効化 → デバッグオプションの削除 → カーネルパラメータの削除の順）
  - 各段階での確認項目（外部ディスプレイの接続/切断テスト、フリッカー確認、サスペンド復帰テスト）を記載する
  - 問題が再発した場合の復元手順を含める
  - _Requirements: 3.3_

- [x] 6. 設定の整合性検証
- [x] 6.1 変更後のconfig.kdlが `niri validate` コマンドでエラーなく通ることを確認する
  - 全ての設定変更が適用された状態で設定検証を実行する
  - エラーがあれば修正して再検証する
  - _Requirements: 1.1, 1.2, 1.3, 2.1_
